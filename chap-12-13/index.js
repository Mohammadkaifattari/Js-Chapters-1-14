// 1. Write a program that takes a character (number or string) in a variable & checks whether the given input is 
// a number, uppercase letter or lower case letter. (Hint: ASCII codes:- A=65, Z=90, a=97, z=122)
// let char = prompt("کوئی ایک کریکٹر درج کریں:");

// if (char.length !== 1) {
//     console.log("براہ کرم صرف ایک کریکٹر درج کریں۔");
// } else {
//     let asciiCode = char.charCodeAt(0); // ASCII کوڈ حاصل کریں

//     if (asciiCode >= 48 && asciiCode <= 57) {
//         console.log("یہ ایک نمبر ہے۔");
//     } else if (asciiCode >= 65 && asciiCode <= 90) {
//         console.log("یہ ایک بڑا حرف (Uppercase letter) ہے۔");
//     } else if (asciiCode >= 97 && asciiCode <= 122) {
//         console.log("یہ ایک چھوٹا حرف (Lowercase letter) ہے۔");
//     } else {
//         console.log("یہ نہ نمبر ہے نہ انگریزی کا حرف۔");
//     }
// }

// Write a JavaScript program that accept two integers and display the larger. Also show if the two integers are equal.
// 3. Write a program that takes input a number from user & state whether the number is positive, negative or zero.
// 4. Write a program that takes a character (i.e. string of length 1) and returns true if it is a vowel, false otherwise
// 5. Write a program that
// a. Store correct password in a JS variable.
// b. Asks user to enter his/her password
// c. Validate the two passwords:
// i. Check if user has entered password. If not, then give message “ Please enter your password”
// ii. Check if both passwords are same. If they are same, show
// var num1=+prompt("num1")
// var num2=+prompt("num2")

// if (num1>num2) {alert("frist number is Greater than 2nd num")

// }
// else
// (alert("2nd number is Greater than frist num"))

// 3. Write a program that takes input a number from 
// user & state whether the number is positive, negative or zero.



// var num1=+prompt("num1")


// if (num1>0) {alert("positive")

// }
//  else if (num1<0) {alert("Negative")

// }
// else (alert("zero"))

// 4.m that takes a character (i.e. string of length 1) and returns 
// true if it is a vowel, false otherwise Write a progra



// var vowels =["a","e","i","o","u"]
// var input=prompt("check letter").toLowerCase()
// var output=false;
// for (let i = 0; i < vowels.length; i++) {
//   if (input===vowels[i]) { output=true;
//     break;



// }


// }
// if (output===true) {alert("Vowel")

// }
// else{alert(output)}


// 5. Write a program that
// a. Store correct password in a JS variable.
// b. Asks user to enter his/her password
// c. Validate the two passwords:
// i. Check if user has entered password. If not, then give message “ Please enter your password”
// ii. Check if both passwords are same. If they are same, show message “Correct! The password you entered matches the original password”. Show “Incorrect password” otherwise.


// var correctpass = "grand operation"
// var input = prompt("input pass")
// var valid = ""

// if (input === valid) {
//   alert(input = prompt("Please enter your password"))

// }
// else if (input === correctpass) {
//   alert("Grand Operation SuccesFull")

// }
// else { alert("JhAZ Jhangi tha") }

// 6. This if/else statement does not work. 
// Try to fix it: var greeting; var hour = 13; if (hour < 18) 
//   { greeting = "Good day"; else greeting = "Good evening"; }
// var greeting;
// var hour=new Date()

// if (hour < 18) {alert( greeting = "Good day") }
//   else {alert(greeting = "Good evening") }
  